// Randomly generated TypeScript code

function getRandomNumber(max: number): number {
  return Math.floor(Math.random() * max);
}

const numbers = [0, 1, 2, 3, 4, 5];
numbers[getRandomNumber(numbers.length)];
